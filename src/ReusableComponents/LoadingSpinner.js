import loadingSpinnerImg from '../assets/images/loadingSpinner.svg';

const LoadingSpinner = () => {
  return (
    <img
      src={loadingSpinnerImg}
      alt="loadingSpinner"
      className="loadingSpinner"
    />
  );
};
export default LoadingSpinner;
